# Mobile App Environment Configuration
# Copy this file to .env.local and update the values for your local development

# === API Configuration ===

# Server Port (default: 12000)
EXPO_PUBLIC_SERVER_PORT=12000

# Option 1: Auto-detect from Expo hostUri (recommended for development)
# The app will automatically extract the IP from the Expo connection
# No need to set this - it's detected automatically when running 'npx expo start'

# Option 2: Manual IP override (optional, for specific network scenarios)
# Set this if auto-detection doesn't work or you want to force a specific IP
# Example: EXPO_PUBLIC_LOCAL_IP=192.168.1.95
# EXPO_PUBLIC_LOCAL_IP=

# Option 3: Complete API URL override (highest priority)
# Set this if you want full control over the API endpoint
# Example: EXPO_PUBLIC_API_URL=http://192.168.1.95:12000
# EXPO_PUBLIC_API_URL=

# === Expo Configuration ===

# Debug mode (optional)
# EXPO_DEBUG=true
